{
  "name": "petecho-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n frontend,backend -c cyan,yellow \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "concurrently -n frontend,backend -c cyan,yellow \"npm start --prefix frontend\" \"npm start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "react-markdown": "^10.1.0"
  }
}
